let switchCounter = numberOfSwitches
var gemCounter = 0
while switchCounter != gemCounter {
    moveForward()
    if isOnGem {
        collectGem()
        gemCounter = gemCounter + 1
    }
    if isBlocked {
        turnRight()
    }
}
